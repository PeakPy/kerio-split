name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  shell-syntax:
    name: Shell syntax
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: bash -n
        run: |
          set -euo pipefail
          while IFS= read -r f; do
            echo "check $f"
            bash -n "$f"
          done < <(
            find platforms/macos/Scripts scripts -type f \( \
              -name '*.sh' -o -name 'keriosplit-ctl' \
              -o -name 'preinstall' -o -name 'postinstall' \
            \) 2>/dev/null
          )

  config-example:
    name: Example config JSON
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m json.tool config/config.example.json > /dev/null

  version-sync:
    name: VERSION is single source
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Info.plist and fallback match VERSION
        run: |
          set -euo pipefail
          V="$(tr -d '[:space:]' < VERSION)"
          test -n "$V"
          grep -F "<string>${V}</string>" platforms/macos/KerioSplit/Info.plist | wc -l | grep -q '^[ ]*2$'
          grep -F "?? \"${V}\"" platforms/macos/KerioSplit/TunnelController.swift
          grep -F "__version__ = \"${V}\"" core/keriosplit/__init__.py
          echo "VERSION=$V OK"

  core-tests:
    name: Shared core (dry-run)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: unit tests
        run: make test-core

  linux-docker-smoke:
    name: Linux route smoke (Docker)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: docker smoke
        run: make test-linux-docker
